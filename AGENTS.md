# プロジェクトルール

## コミット規約
- コミットするときの変更メモ（コミットメッセージ）は日本語で記述してください。

## 開発規約
- 特に指定がない限り、言語は C#、フレームワークは .NET 10 を使用すること。
- GUI アプリケーションの場合は、終了時のウィンドウ位置およびサイズを保存し、次回起動時に復元すること。
- 設定があるプログラムは、設定を保存できるようにすること。
- 設定ファイルの保存先は `AppData\Local\(アプリ名)`（例: `C:\Users\<ユーザー名>\AppData\Local\ImageManager`）とすること。
- ソースコードには処理内容や意図が分かるコメントを適切に記述すること。
- 設定ファイル内の日本語はUnicodeエスケープ（`\uXXXX` 等）せず、そのまま可視テキスト（UTF-8）として保存・管理できるようにすること（例: `Encoder = JavaScriptEncoder.UnsafeRelaxedJsonEscaping` を指定するなど）。
- 多言語（日本語・英語）に対応し、Windowsの言語設定（表示言語モード）に応じて表示言語を切り替えること。

## バージョン管理規約
- アプリケーションにはアプリバージョンおよびファイルバージョンを付与し、未定義の場合は `1.0.0.0` から始めること。
- 「バージョンを一つ上げて」と指示された場合は、`a.b.c.d` 形式の `c` の数字を1つ上げること（例: `1.0.0.0` → `1.0.1.0`）。
- 機能を追加・向上させた際や不具合を修正するたびに、バージョン番号（Version `a.b.c.d`）のビルド番号（`d`、末尾の `x`）を1つ上げること（例: `1.0.0.0` → `1.0.0.1`）。

## アイコン・リソース規約
- アプリケーションにはプログラムの動作・目的に沿ったアイコンを作成・設定すること。

## ビルド・パッケージ規約
- ビルドする際は、実行環境（パソコン）のアーキテクチャ（x64、Arm64 等）に合わせたものを作成すること。
- スタンドアロンインストーラは exe 形式で `.\Installer` フォルダに作成し、ファイル名にはバージョン番号を含めること。
- `Installer` フォルダ（`.\Installer`）は Git のコミット対象外（`.gitignore` に追加）とすること。
- Microsoft Store に登録する場合は `.\MSIX` フォルダに msix ファイルを作成すること。アーキテクチャは x64 と Arm64 を作成すること。
- `MSIX` フォルダ（`.\MSIX`）は Git のコミット対象外（`.gitignore` に追加）とすること。
